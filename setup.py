"""
THIS FILE ALLOWS USER TO INSTALL PACKAGES DEFINED IN THE REPO
"""

from setuptools import setup, find_packages

setup(
    name="jpaludise",
    version="0.1.0",
    packages=find_packages(),
)
